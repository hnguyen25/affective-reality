"""
PREPROCESSING
Huy Nguyen (2021)

These functions takes the data obtained from the __dataloaders__ module and pre-processes it into a form which can be easily plugged into a classification algorithm.

"""

def normalize_data(data, normalize_mean=True, normalize_variance=True):
    """

    Args:
        data:
        normalize_mean:
        normalize_variance:

    Returns:

    """
    return None

def basenorm_data(data, baseline):
    """

    Args:
        data:
        baseline:

    Returns:

    """
    return None

def spatialize_data(data, eeg_matrix):
    """

    Args:
        data:
        eeg_matrix:

    Returns:

    """
    return None

def window_data(data, window_len=8000):
    """

    Args:
        data:
        window_len:

    Returns:

    """
    return None

def create_topomap(data, cmap, image_width=200):
    """

    Args:
        data:
        cmap:
        image_width:

    Returns:

    """
    return None
